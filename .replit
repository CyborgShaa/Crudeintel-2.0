run = "pip install -r requirements.txt && streamlit run app.py --server.port 8080 --server.address 0.0.0.0"

[nix]
channel = "stable-21_11"

[deployment]
run = ["sh", "-c", "pip install -r requirements.txt && streamlit run app.py --server.port 8080 --server.address 0.0.0.0"]
